export interface MetricResponse {
  id: number;
  name: string;
  parameters: string;
  isSelected: boolean;
}

export interface AggregationResponse {
  id: number;
  name: string;
  isSelected: boolean;
}